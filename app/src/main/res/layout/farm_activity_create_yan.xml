<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.farm.innovation.biz.insurance.CreateYanActivity">

    <include layout="@layout/farm_activity_title" />

    <LinearLayout
        style="@style/form_style.layout"
        android:layout_marginTop="20dp">

        <TextView
            style="@style/form_style.text"
            android:text="验标名称:" />

        <EditText
            android:id="@+id/baodanApplyName"
            android:layout_width="170dp"
            android:layout_height="match_parent"
            android:inputType="text"
            android:hint="请输入验标名称"
            android:maxLength="50"
            android:maxLines="2"
            android:textSize="16sp" />

    </LinearLayout>


    <Button
        android:id="@+id/btnyanNext"
        android:layout_width="120dp"
        android:layout_height="40dp"
        android:layout_gravity="center"
        android:layout_marginTop="100dp"
        android:background="@drawable/farm_button_background"
        android:text="下一步"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="300dp"
        android:layout_height="40dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="100dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btncaiji"
            android:layout_width="120dp"
            android:layout_height="40dp"
            android:background="@drawable/farm_button_onclick"
            android:text="图像采集"
            android:layout_weight="1"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:visibility="gone" />

        <Button
            android:id="@+id/btnyanWan"
            android:layout_width="120dp"
            android:layout_height="40dp"
            android:layout_marginLeft="20dp"
            android:layout_weight="1"
            android:background="@drawable/farm_button_onclick"
            android:text="完成"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:visibility="gone" />
    </LinearLayout>

</LinearLayout>
